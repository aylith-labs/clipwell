<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="Clipwell.Ui.DiagnosticsWindow"
        Title="Clipwell Diagnostics"
        Width="520" Height="600"
        WindowStartupLocation="CenterScreen"
        CanResize="True">
    <Border Background="{DynamicResource ClipCardBackground}" Padding="24">
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="Diagnostics" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,14" />

            <Grid DockPanel.Dock="Bottom" ColumnDefinitions="*,Auto,Auto,Auto" Margin="0,14,0,0">
                <TextBlock Grid.Column="0" Name="StatusText" FontSize="12" Opacity="0.7" VerticalAlignment="Center" />
                <Button Grid.Column="1" Name="RefreshButton" Content="Refresh" Padding="14,6" Margin="0,0,10,0" />
                <Button Grid.Column="2" Name="ClearButton" Content="Clear perf log" Padding="14,6" Margin="0,0,10,0" />
                <Button Grid.Column="3" Name="CloseButton" Content="Close" Padding="14,6" />
            </Grid>

            <ScrollViewer>
                <StackPanel Spacing="18">
                    <StackPanel Spacing="6">
                        <TextBlock Classes="section" Text="DAEMON" />
                        <TextBlock Name="DaemonText" FontSize="12.5"
                                   FontFamily="{DynamicResource ClipFontMono}" TextWrapping="Wrap" Opacity="0.85" />
                    </StackPanel>

                    <StackPanel Spacing="6">
                        <TextBlock Classes="section" Text="PICKER SHOW-CYCLE (PERF.LOG)" />
                        <TextBlock Name="PerfSummary" FontSize="12.5" Opacity="0.85" />
                        <Border Background="{DynamicResource ClipSurface1}"
                                CornerRadius="{DynamicResource ClipRadiusM}" Padding="10" Height="320">
                            <ScrollViewer>
                                <ItemsControl Name="PerfList">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" FontSize="12"
                                                       FontFamily="{DynamicResource ClipFontMono}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Border>
</Window>
